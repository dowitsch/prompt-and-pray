node_modules

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit
/build

# OS
.DS_Store
Thumbs.db

# Env
.env
.env.*
!.env.example
!.env.test

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# Secrets: this file ships live API keys and must never be committed.
# Configure them through .env instead — see docs/ai-integration.md.
doc/apertus-*.md

# Local dev artefacts
screenshots/
# The SQLite database is per-environment; `npm run db:migrate && npm run db:seed`
# rebuilds it. On Fly it lives on a volume at /data.
data/
# Scratch worktrees. Left in place they shadow the tsconfig root and eslint
# reports a parse error for every file in the project.
.claude/worktrees/
